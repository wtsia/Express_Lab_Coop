<h1>{{instance.title}}</h1>


<a href="{{instance.url}}">{{instance.url}}</a>
<h4>{{instance.description}}</h4>
<h5>{{instance.date}}</h5>

<a href="/edit/{{instance.id}}">Edit</a>


{{!-- add a delete button --}}
<form action="/{{instance.id}}?_method=delete" method="post">
    <input type="submit" value="Delete">
</form>